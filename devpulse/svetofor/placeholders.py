"""Заглушки для метрик без готового расчёта.

Каждая функция здесь — ВРЕМЕННАЯ. Когда появится реальный расчёт, замените
тело функции — интерфейс (что возвращает) менять не нужно, светофор подхватит.
Возврат None = «нет данных» → метрика станет серой, не сломает светофор.
"""
from __future__ import annotations


def data_quality_score(project: str) -> float | None:
    """Чистота данных подразделения, % (0..100).
    ЗАГЛУШКА: подсистема правил «хороший/плохой эпик» ещё не построена.
    TODO: считать из правил гигиены (заполненность обяз. полей, декомпозиция,
    достоверность движения по статусам). Пока возвращаем None."""
    return None
